# LearningOS Frontend

React application for LearningOS v0.1.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_URL` in `.env` when the API is not running at `http://localhost:8000/api/v1`.
