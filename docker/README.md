# Docker

Release v0.1 uses root-level `docker-compose.yml` plus service-specific Dockerfiles in `frontend/` and `backend/`.

This folder is reserved for future local Docker assets such as nginx configuration or database init scripts.
